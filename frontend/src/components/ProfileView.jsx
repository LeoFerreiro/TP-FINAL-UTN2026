import { CheckCircle, Envelope, ShieldCheck, Target } from "@phosphor-icons/react";

export function ProfileView({ user, tasks, categories, onLogout }) {
  const completed = tasks.filter((task) => task.status === "completed").length;
  return <section className="page-view profile-view"><div className="page-heading"><div><p className="eyebrow">Tu cuenta</p><h2>Perfil</h2><p>Información de acceso y resumen de actividad.</p></div></div><div className="profile-layout"><section className="profile-card profile-card--identity"><div className="profile-avatar">{user.name.split(" ").map((part) => part[0]).slice(0, 2).join("")}</div><h3>{user.name}</h3><p><Envelope />{user.email}</p><span><ShieldCheck />Correo verificado</span><button className="button button--ghost button--full" onClick={onLogout}>Cerrar sesión</button></section><section className="profile-card"><h3>Tu actividad</h3><div className="profile-stats"><article><Target /><strong>{tasks.length}</strong><span>Tareas totales</span></article><article><CheckCircle /><strong>{completed}</strong><span>Completadas</span></article><article><span className="category-symbol" /><strong>{categories.length}</strong><span>Categorías</span></article></div><div className="profile-progress"><div><strong>Progreso general</strong><span>{tasks.length ? Math.round(completed / tasks.length * 100) : 0}%</span></div><i><b style={{ width: `${tasks.length ? completed / tasks.length * 100 : 0}%` }} /></i></div></section></div></section>;
}
